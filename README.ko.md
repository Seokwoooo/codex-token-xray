[English](./README.md) | **한국어**

# codex-token-xray

내 Codex 세션에서 컨텍스트가 어디에 쓰였는지 보여주고 줄일 수 있는 것만 줄여주는 스킬입니다.

## 설치

```bash
npx skills add Seokwoooo/codex-token-xray
```

Codex 입력창에 이것만 치면 됩니다.

```text
$codex-token-xray
```

모든 프로젝트에서 쓰려면 `-g`를 붙여서 설치합니다. Python 3.9 이상이면 됩니다.

## 실행하면 이렇게 보입니다

![Codex 세션 하나가 컨텍스트를 쓰는 곳](docs/where-tokens-go-ko.svg)

한 컴퓨터의 실제 세션 12개를 읽은 결과입니다.

- 세션은 시작만 해도 약 33,000토큰을 씁니다. Codex 앱 지침 12,000과 기본 지침 5,000과 스킬 목록 6,000이 그 안에 있습니다. 이건 사용자가 줄일 수 없는 부분입니다.
- 작업 중에는 도구가 돌려준 결과가 절반을 넘습니다. 파일을 통째로 읽을 때마다 그대로 쌓입니다.
- 스킬 설명은 세션당 39토큰입니다. 그런데 그 스킬의 본문은 19번 읽히면서 36,000토큰을 썼습니다. 줄여서 효과가 나는 건 본문입니다.

## 그래서 무엇을 하나

딱 세 가지만 합니다.

- 어떤 세션에서도 읽히지 않은 스킬을 지웁니다. 폴더 전체를 zip으로 백업한 뒤에 지우고 명령 하나로 되돌립니다.
- 내가 만든 긴 스킬을 줄입니다. 매번 필요한 내용만 SKILL.md에 남기고 나머지는 `references` 파일로 옮깁니다. 지우는 것은 없습니다.
- 프로젝트 AGENTS.md도 같은 방식으로 줄입니다.

바꾸기 전에 diff를 보여주고 한 번 묻습니다. 남이 만든 스킬과 Codex 기본 스킬과
플러그인은 절대 고치지 않습니다. 만든 사람이 계속 다듬고 있고 업데이트하면
수정이 덮어써지기 때문입니다. 안 쓰는 스킬이면 보고서가 알려주니 지우면 됩니다.

## 숫자를 믿어도 되나

- 토큰 합계는 Codex가 남긴 사용량 기록 그대로입니다.
- 항목별 나누기는 크기로 추정합니다. 실측과 비교하면 0.85 수준으로 맞았고 보고서에 그 비율이 같이 찍힙니다.
- 줄인 효과는 그 뒤에 시작한 세션부터 보입니다.

<details>
<summary>더 알고 싶다면</summary>

- 세션 로그는 `~/.codex/sessions`에서 읽습니다. 네트워크는 쓰지 않습니다.
- 현재 폴더에서 시작한 세션만 보려면 `--project`를 붙입니다.
- 검사 결과와 백업은 `~/.codex-token-xray` 아래에만 둡니다.
- [astra-xray](https://github.com/Seokwoooo/astra-xray)는 Astra에 맞게 스킬 목록과 지침을 정리하는 스킬이고 이 스킬은 모델과 상관없이 토큰이 어디로 가는지 잽니다.
- 개발: `python3 -m unittest discover -s tests`
- 라이선스: MIT

</details>
